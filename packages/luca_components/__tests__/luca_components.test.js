'use strict';

const lucaComponents = require('..');
const assert = require('assert').strict;

assert.strictEqual(lucaComponents(), 'Hello from lucaComponents');
console.info('lucaComponents tests passed');
